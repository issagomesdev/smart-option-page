/**
 * Copy for the loading screen. English on purpose, matching the technical register the landing
 * already uses in `HERO_BADGE_TEXT` ("Telegram Bot • API REST • Admin Dashboard • PIX Automation")
 * rather than the Portuguese prose — and it names the product instead of describing the wait,
 * which is what makes a splash read as brand rather than as a spinner with a caption.
 */
export const LOADING_LABEL = 'Preparing Smart Option...'
